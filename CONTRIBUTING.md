# 贡献规范

感谢对BrewIon的关注。本项目接受代码、数据、测试、文档和兼容性修复。

## 可提交内容

- 咖啡产地、产区、庄园、处理站数据；
- 豆种、处理法、风味和别名；
- 实体关系和置信度修订；
- 浏览器兼容性修复；
- 二维码生成、扫码和CRC问题；
- 图片裁切和本地存储问题；
- 文档、测试用例和部署说明；
- 不破坏旧码的性能和界面改进。

## 提交前要求

### 代码修改

至少完成：

1. 页面正常加载；
2. 必填字段完成后生成二维码；
3. 编码可解码回原字段；
4. CRC校验通过；
5. 篡改数据能够被CRC拦截；
6. 无自定义字段时保持固定结构；
7. 自定义字段可以还原；
8. CJK16中文烘焙商可以还原；
9. 图片上传、裁切、重绘和本地恢复正常；
10. 日期边界测试通过；
11. 清空表单不删除烘焙商和中心图片；
12. 浏览器控制台无未处理错误。

请说明测试浏览器、版本、操作步骤和结果。仅提供“代码可运行”或“静态检查通过”不构成充分测试。

### 数据修改

每一项事实性新增或修订必须给出可追溯来源。

优先使用：

- 生产者、庄园和处理站官方资料；
- 政府、行业组织和科研机构；
- 可验证的供应链资料；
- 多来源交叉验证。

商业页面和二手媒体可以作为辅助证据，但通常不应作为唯一依据。

不确定映射必须降低`confidence`，不得伪装成确定关系。

## 禁止事项

- 删除、插入或重排既有索引行；
- 用重新排序后的JSON覆盖官方编码表；
- 修改既有代码使其代表另一个实体；
- 把自定义字段自动写入官方数据库；
- 在没有迁移方案时改变固定字段宽度；
- 移除CRC或绕过校验；
- 用文本二维码生成函数重新编码原始二进制字节；
- 在未做浏览器测试时声称功能已经跑通；
- 把未经维护者确认的Fork描述为官方版本。

## Pull Request内容

PR说明应包括：

```text
修改目的：
涉及文件：
数据来源：
兼容性影响：
旧二维码影响：
测试环境：
测试步骤：
测试结果：
已知限制：
回退方法：
```

建议一个PR只处理一个主题。代码修改和大规模数据修改应拆分提交。

## Issue要求

错误报告尽量提供：

- 页面版本；
- 浏览器和系统；
- 操作步骤；
- 实际结果；
- 预期结果；
- 页面状态提示；
- 控制台错误；
- 可复现的HEX编码或测试数据；
- 是否使用中心图片、自定义字段或远程编码表。

涉及隐私的豆单、图片和内部供应链资料应先脱敏。

## 审核和合并

社区提交属于建议，不构成自动合并承诺。

维护者可：

- 要求补充证据或测试；
- 要求拆分提交；
- 修改标题、文档和显示文字；
- 拒绝破坏兼容性的方案；
- 延后有争议数据；
- 回退已合并但被证明错误的内容；
- 决定正式发布时间和版本号。

Pull Request是否合并，由仓库维护者最终决定。
