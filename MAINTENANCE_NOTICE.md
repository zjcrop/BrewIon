# BrewIon维护公告

**公告版本：v3.0**  
**生效日期：2026-07-27**

本公告说明BrewIon官方仓库、咖啡二维码工具和公开编码表的维护边界。

## 一、官方范围

官方仓库：

```text
https://github.com/zjcrop/BrewIon
```

官方在线工具：

```text
https://zjcrop.github.io/BrewIon/coffee-qr-tool/
```

当前正式工具版本为 **v3.0**。本次恢复README和维护文档，不改变工具版本，不修改二维码协议，也不提升编码表版本。

## 二、维护对象

维护内容包括：

- `coffee-qr-tool/index.html`；
- `coffee-qr-codebook/coffee_qr_codebook_v6.json`；
- 项目README和技术规范；
- 数据关系、别名和状态；
- 浏览器兼容性；
- 生成、下载、扫码和解码链路；
- 发布和回退记录。

## 三、稳定性承诺

本项目将优先维护以下稳定性：

1. 已发布数据库行号；
2. 固定字段边界；
3. CJK16烘焙商解码；
4. 自定义UTF-8扩展边界；
5. CRC16校验；
6. 日期编码可逆性；
7. 旧二维码映射不被新数据重排破坏。

“整理数据库”“按名称排序”“删除重复项”等操作不得凌驾于旧二维码兼容性之上。

## 四、自定义信息政策

v3.0允许用户在二维码中写入自定义国家、产区、庄园、处理站、豆种、处理法、风味和备注。

这些内容：

- 仅存在于用户二维码；
- 不自动上传到仓库；
- 不自动分配官方索引；
- 不改变官方编码表；
- 不构成官方数据收录承诺。

需要纳入官方编码表时，应通过Issue或Pull Request提交证据。

## 五、数据维护原则

- 事实性信息必须可追溯；
- 翻译优先保留原文和常见译名；
- 同名实体先区分，不轻率合并；
- 不确定关系标注较低置信度；
- 候选数据可以保留，但不能伪装为已确认；
- 旧条目停用时保留索引行；
- 新增索引只能追加到末尾。

## 六、发布规则

正式发布前至少完成：

- 编码和解码回归；
- CRC标准向量和篡改测试；
- 中文、英文和混合烘焙商测试；
- 无自定义和有自定义两套测试；
- 日期边界测试；
- 图片上传和本地恢复；
- GitHub Pages实际访问检查；
- 版本显示、文件哈希和部署路径检查。

文档修订、错别字和维护公告恢复不自动提升工具版本。协议、功能或行为变化是否提升版本，由维护者评估。

## 七、GitHub Pages维护

正式发布目录：

```text
main / (root)
```

页面路径：

```text
coffee-qr-tool/index.html
```

如果仓库文件已更新但公开页面仍是旧版，可在Settings → Pages中：

1. 将发布Branch临时设为None并保存；
2. 再恢复为`main / (root)`并保存；
3. 检查公开页面源代码和版本号。

## 八、最终审核权

维护者对以下事项保留最终审核、解释、合并、回退和发布权：

- 官方数据是否收录；
- PR是否合并；
- 官方版本号；
- 兼容性争议；
- 发布日期；
- 写入权限；
- 错误数据撤回；
- 官方品牌和项目名称使用。

社区贡献受到尊重，但提交不等于自动采纳。

## 九、Fork和衍生项目

许可证允许Fork和修改。衍生项目应：

- 保留许可证和源码义务；
- 标明修改内容；
- 不冒用官方发布身份；
- 不将不同索引顺序的编码表描述为官方兼容表；
- 明确其二维码协议和数据库来源。
